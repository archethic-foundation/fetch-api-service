import Config

IO.puts("=================================================")
IO.puts("== RUNNING IN DEV MODE, USING THE SANDBOX APIS ==")
IO.puts("=================================================")

# using test config as dev
import_config("test.exs")
